import { GroupBox } from "std-widgets.slint";
export component Output inherits Text {
    color: black;
    font-weight: 550;
    horizontal-alignment: center;
    vertical-alignment: center;
}

export component OutputModal inherits GroupBox {
    width: 250px;
    height: 120px;
    padding: 20px;
}
